# Telemetry Feature Extraction

This directory maps raw PMU performance counters and scheduling state into the 16 fixed-point features consumed by the model:
$$\mathbf{s}_i(t) = \left[ c_i(t), \hat{b}_i(t), w_i(t), \mu_{cache}(i), \beta_{br}(i), \dots \right]^T$$

## Status: `SCAFFOLDED` (Phase 2, Week 3)
