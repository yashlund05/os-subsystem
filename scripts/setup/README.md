# Environment Setup Scripts

This directory houses environment initialization scripts:
- `bootstrap_linux_kernel.sh`: Verifies Linux kernel version $\ge 6.12$ and `sched_ext` headers.
- `install_python_deps.sh`: Configures Python virtual environment with PyTorch and TensorRT.
