# Performance & Latency Regression Suite

This directory contains automated latency regression tests measuring micro-inference latency via hardware TSC (`rdtsc_ordered`).

## Target Bound: $\le 45\text{ ns}$ (< 150 cycles @ 3.0 GHz)
## Guardrail Trip Threshold: $\ge 50\text{ ns}$

## Status: `SCAFFOLDED` (Phase 2, Week 4)
