# eBPF Telemetry Tracepoints

This directory contains Linux eBPF telemetry hooks attaching to kernel context switch and CPU runqueue tracepoints (`sched_switch`, `sched_wakeup`).

## Status: `NOT IMPLEMENTED` (Scheduled for Phase 1, Week 1)
